import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function UnlockScreen() {
    return (
        <PlaceholderScreen
            title="Unlock Allverb"
            message="A one-time purchase removes ads and unlocks the full library."
            feature="F15"
        />
    );
}
